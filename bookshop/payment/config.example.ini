; =====================================================================
; eSewa payment configuration (TEMPLATE)
;
; Copy this file to `config.ini` in the same directory and fill in your
; real merchant credentials. `config.ini` is git-ignored and must never
; be committed.
;
; Values may also be supplied through environment variables, which take
; priority over this file:
;   ESEWA_MERCHANT_CODE, ESEWA_SECRET_KEY, ESEWA_ENVIRONMENT, ESEWA_APP_BASE_URL
;
; Environment values:
;   sandbox     -> https://rc-epay.esewa.com.np (eSewa test environment)
;   production  -> https://epay.esewa.com.np
;
; Sandbox/test credentials below are eSewa's OFFICIAL public test values
; published at https://developer.esewa.com.np - they are safe for local
; development only. NEVER use them in production.
; =====================================================================

ESEWA_MERCHANT_CODE = EPAYTEST
ESEWA_SECRET_KEY    = 8gBm/:&EnhH.1/q
ESEWA_ENVIRONMENT   = sandbox

; Absolute base URL of the bookshop application (no trailing slash).
; For phone/tunnel testing replace with e.g. https://xxxx.in.ngrok.io/bookshop
ESEWA_APP_BASE_URL  = http://localhost/ebookstore/bookshop

; Tax / service charge applied on products (0 if not applicable).
ESEWA_TAX_AMOUNT          = 0
ESEWA_SERVICE_CHARGE      = 0
